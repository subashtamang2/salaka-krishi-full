import samplePage from './page';
import PageList from './pageList';
import { NavItemType } from 'types/menu';


const menuItems: { items: NavItemType[] } = {
    items: [samplePage, PageList]
};

export default menuItems;
